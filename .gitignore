# Dependencies & build output
node_modules/
out/
build/*
!build/update-channel.txt
*.log
*.blockmap

# Env & OS
.env
.env.*
!.env.example

# Multimodal benchmark artifacts (may contain raw model output)
landing/scripts/multimodal_bench/benchmark_results/
landing/scripts/multimodal_bench/.env
landing/scripts/multimodal_bench/dataset/images/
.DS_Store
Thumbs.db

# Local archives
*.zip

# Development and test artifacts
.test-cache/
.test-tmp/
.natively-*.txt
.npm-cache/
.cache/
onnx-community/
debug-out.png
t1.png
t2.png
logo.prepared.tmp.png
eng.traineddata

# Editor
.idea/
.vscode/
*.swp
.vercel/

# Desktop build output (never commit — Vercel ignores dist/ via .vercelignore)
dist/
dist-build-*/
dist-fresh-*/
dist-interview-*/
dist-release/

# Graphify knowledge graph (local dev artifact)
graphify-out/
!build/version-state.json

# Rust native audio addon (keep committed .node prebuilds if present)
native/veilassist-audio/target/
native/veilassist-audio/node_modules/
native/veilassist-audio/*.node.bak
